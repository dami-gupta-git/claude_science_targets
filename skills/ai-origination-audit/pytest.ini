[pytest]
# Pins rootdir to this skill directory. Without a config file here, pytest
# searches upward for one and reads the org-level pyproject.toml, which is
# unrelated to this suite and may not be readable.
testpaths = tests
addopts = -q
